<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mongo.smart_study.mapper.ClassMapper">
    <select id="getClasses" resultType="Class">
        select * from class
    </select>
    <select id="getClassById" resultType="Class">
        select * from class where id=#{id}
    </select>
    <insert id="addClass">
        insert into class (name, rate, origin, description, video_url, post_url, likes, comments_num, watch_num)values (#{name},#{rate},#{origin}, #{description}, #{video_url}, #{post_url}, #{likes}, #{comments_num}, #{watch_num})
    </insert>

</mapper>