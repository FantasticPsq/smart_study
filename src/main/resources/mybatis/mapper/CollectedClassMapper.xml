<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mongo.smart_study.mapper.CollectClassMapper">
<insert id="addCollectClass" parameterType="UserCollectedClass">
    insert into collect_class (class_id, user_id, username, join_time) values (#{classId},#{userId},#{userName},#{joinTime})
</insert>
    <select id="getUserCollectedClassById" resultType="UserCollectedClass">
        select * from collect_class where user_id=#{id}
    </select>
    <select id="check" parameterType="UserCollectedClass" resultType="UserCollectedClass">
        select * from collect_class where user_id=#{userId} and class_id=#{classId}
    </select>
    <delete id="deleteCollectClass" parameterType="UserCollectedClass">
        delete from collect_class where user_id=#{userId} and class_id=#{classId}
    </delete>
</mapper>